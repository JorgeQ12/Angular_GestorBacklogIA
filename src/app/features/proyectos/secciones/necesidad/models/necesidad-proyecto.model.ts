/** Describe el problema que justifica la creación del proyecto. */
export interface NecesidadProyecto {
  readonly situacionActual: string;
  readonly problemas: string;
  readonly impacto: string;
}
