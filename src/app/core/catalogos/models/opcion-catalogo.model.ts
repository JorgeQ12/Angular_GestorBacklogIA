/** Representa una opción activa utilizable por los controles de la aplicación. */
export interface OpcionCatalogo {
  readonly id: number;
  readonly nombre: string;
  readonly descripcion: string;
}
